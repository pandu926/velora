{
  "name": "defi-autopilot-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous AI-powered DeFi portfolio manager using MetaMask Smart Accounts",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
